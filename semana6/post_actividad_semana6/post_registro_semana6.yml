info:
  name: post_registro_semana6
  type: http
  seq: 1

http:
  method: POST
  url: "http://localhost:3000/registro "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​